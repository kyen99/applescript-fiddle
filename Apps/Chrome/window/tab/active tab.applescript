tell application "Google Chrome"
	if (count of windows) > 0 then
		tell front window
			--active tab
			get properties of active tab
			--set active tab index to 3
			tell front tab
			
			end tell
		end tell
	end if
end tell